.edittest
  .title-newtest
    = link_to 'Publisher', publisher_path(@publisher), {class: 'link_back'}
    span Edit test "#{@publishers_test.title}"

  = render "form", isEdit: true

  /= link_to "Delete", @publishers_test, data: {:confirm => 'Are you sure?'}, :method => :delete
  = link_to "Delete", @publishers_test, {class: "btn btn_red delete-edittest js-test-delete", "data-tid" => @publishers_test.id, "data-pid" => @publishers_test.author.id}
