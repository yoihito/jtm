(function( window, $ ) {

    var app = window.app;

    app.routes = {

        test: {
            save: '/tests/pass/save',
            saveAfter: '/tests/result/:id'
        }

    };

})( window, jQuery );
